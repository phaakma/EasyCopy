"""Change detection engine (stub)."""

from typing import Any


def detect_changes(*, source: Any, target: Any) -> dict:
    """Placeholder change detection that returns an empty changeset."""
    return {"adds": [], "updates": {}, "deletes": {}}
